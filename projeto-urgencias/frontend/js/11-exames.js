inicializarPagina('Exames','EXAMES');

conteudo.innerHTML=`<div class="workspace-2col"><div class="main-list"><section class="page-header"><div><h2>Exames</h2><p>Lista de espera de exames e exames realizados.</p></div><div class="page-actions"><input id="pesquisa" class="search-input" placeholder="Pesquisar utente ou exame..." oninput="carregar()"><button onclick="carregar()">↻ Atualizar</button></div></section><div class="filter-tabs"><button>Todos <span id="totalExames">0</span></button><button class="orange">Em espera</button><button class="green">Realizados</button></div><section class="panel"><h3>Lista de espera</h3><table id="tabela"></table></section><section class="panel"><h3>Exames realizados</h3><table id="tabelaRealizados"></table></section><div class="pagination"><span id="contador">A mostrar 0 exames</span></div></div><aside class="right-details" id="detalhe"></aside></div>`;
let cache=[];
function isExame(a){const t=String(a.tipo||'').toLowerCase();return t && !['consulta','nova_consulta','encerramento'].includes(t)}
function renderDetalhe(item={}){if(!item.id){detalhe.innerHTML='<h2>Sem exame selecionado</h2><p>Selecione um exame na lista.</p>';return}const realizado=String(item.estado||'').toLowerCase().includes('realizado');detalhe.innerHTML=`<a class="close-x">×</a><h2>${valor(item.utente)}</h2><div class="small-id">ID: UTE000${item.episodio_urgencia_id||item.id}</div><span class="badge ${realizado?'prioridade-verde':'prioridade-laranja'}">${realizado?'Exame realizado':'Em lista de espera'}</span><div class="detail-module"><h3>🧪 Exame</h3><p><strong>${valor(item.tipo)}</strong></p><p>Pedido em: ${hora(item.data)}</p><p>Profissional: ${valor(item.profissionais)}</p><p>Estado: <span class="badge">${valor(item.estado)||'pendente'}</span></p>${realizado?'':`<button class="success full-btn" onclick="marcarRealizado(${item.id})">Marcar exame realizado</button>`}</div><a class="btn secondary full-btn" href="5-historico-utentes.html">↻ Ver histórico do utente</a>`}
async function marcarRealizado(id){try{await apiPut('/atos/'+id+'/estado',{estado:'realizado'});await carregar()}catch(e){alert(e.message)}}
async function carregar(){try{cache=filtrarHospital(await apiGet('/atos')).filter(isExame);const q=(pesquisa.value||'').toLowerCase();let dados=cache.filter(a=>(q||String(a.estado_episodio||'').toLowerCase()!=='encerrado')&&(!q||String(a.utente).toLowerCase().includes(q)||String(a.tipo).toLowerCase().includes(q))); const pendentes=dados.filter(a=>!String(a.estado||'').toLowerCase().includes('realizado'));const realizados=dados.filter(a=>String(a.estado||'').toLowerCase().includes('realizado'));totalExames.textContent=dados.length;contador.textContent=`A mostrar ${dados.length} exames`;mostrarTabela('tabela',['Ordem','Utente','Exame','Pedido em','Estado','Profissional'],[(a)=>pendentes.findIndex(x=>x.id===a.id)+1,a=>`<strong>${valor(a.utente)}</strong><br><small>Episódio: ${valor(a.episodio_urgencia_id)}</small>`,'tipo',a=>hora(a.data),a=>`<span class="badge prioridade-laranja">${valor(a.estado)||'pendente'}</span>`,'profissionais'],pendentes,(a)=>`<button class="table-row-action" onclick="renderDetalhe(cache.find(x=>x.id===${a.id}))">›</button><button class="success" onclick="marcarRealizado(${a.id})">Realizado</button>`);mostrarTabela('tabelaRealizados',['Utente','Exame','Pedido em','Estado','Profissional'],[a=>`<strong>${valor(a.utente)}</strong><br><small>Episódio: ${valor(a.episodio_urgencia_id)}</small>`,'tipo',a=>hora(a.data),a=>`<span class="badge prioridade-verde">Realizado</span>`,'profissionais'],realizados,(a)=>`<button class="table-row-action" onclick="renderDetalhe(cache.find(x=>x.id===${a.id}))">›</button>`);renderDetalhe(pendentes[0]||realizados[0]||{})}catch(e){conteudo.innerHTML+=mensagemErro(e)}}carregar();
